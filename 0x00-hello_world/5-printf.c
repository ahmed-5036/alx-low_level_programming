/**
 * main - Entry point
 *
 * Description: This is the main function that uses the 'printf' function to
 * print the text "with proper grammar, but the outcome is a piece of art,"
 * followed by a new line.
 *
 * Return: Always 0 (Success)
 */
#include <stdio.h>
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}

